version: "3.2"

services:
  nestjs:
    build:
      context: ../server
      dockerfile: ../docker/Dockerfile.server
    restart: always
    networks:
      - app-network
    volumes:
      - ../server/src:/app/src
    env_file:
      - ../.env
    ports:
      - 5000:5000

networks:
  app-network:
    driver: bridge
